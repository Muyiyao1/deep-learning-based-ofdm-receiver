# Historical checkpoints

These checkpoints predate the current residual-CNN input schema and configuration fingerprint validation. They are retained only for traceability and are intentionally excluded from formal evaluation.

Use `checkpoints/residual_cnn_stress.pt` for the formal matched experiment, or a separately named checkpoint for quick smoke runs.
